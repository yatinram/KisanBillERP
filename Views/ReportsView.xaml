<UserControl x:Class="KrushiBillERP.Views.ReportsView"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<UserControl.Resources>
		<SolidColorBrush x:Key="PageBg" Color="#F4F6F5"/>
		<SolidColorBrush x:Key="CardBg" Color="#FFFFFF"/>
		<SolidColorBrush x:Key="CardBorder" Color="#E7EBE8"/>
		<SolidColorBrush x:Key="AccentGreen" Color="#2E7D32"/>
		<SolidColorBrush x:Key="MutedText" Color="#6C757D"/>
		<SolidColorBrush x:Key="HeadingText" Color="#1E2422"/>
		<SolidColorBrush x:Key="StatIconBg" Color="#E8F5E9"/>

		<DropShadowEffect x:Key="CardShadow" Color="#000000" Opacity="0.04" BlurRadius="12" ShadowDepth="2" Direction="270"/>

		<Style x:Key="CardStyle" TargetType="Border">
			<Setter Property="Background" Value="{StaticResource CardBg}"/>
			<Setter Property="BorderBrush" Value="{StaticResource CardBorder}"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="CornerRadius" Value="12"/>
			<Setter Property="Effect" Value="{StaticResource CardShadow}"/>
		</Style>

		<!-- ModernCombo (dashboard Style) -->
		<Style x:Key="ModernComboBox" TargetType="ComboBox">
			<Setter Property="Background" Value="White"/>
			<Setter Property="BorderBrush" Value="#E5E5E0"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="FontSize" Value="13"/>
			<Setter Property="Padding" Value="14,0,8,0"/>
			<Setter Property="Foreground" Value="{StaticResource HeadingText}"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="ComboBox">
						<Grid>
							<ToggleButton x:Name="ToggleBtn"
										  Background="{TemplateBinding Background}"
										  BorderBrush="{TemplateBinding BorderBrush}"
										  BorderThickness="{TemplateBinding BorderThickness}"
										  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
										  Focusable="False" ClickMode="Press">
								<ToggleButton.Template>
									<ControlTemplate TargetType="ToggleButton">
										<Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
											<Grid>
												<Grid.ColumnDefinitions>
													<ColumnDefinition Width="*"/>
													<ColumnDefinition Width="26"/>
												</Grid.ColumnDefinitions>
												<ContentPresenter Grid.Column="0" Margin="16,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left" Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}"/>
												<TextBlock Grid.Column="1" Text="&#xE70D;" FontFamily="Segoe MDL2 Assets" FontSize="9" Foreground="#8A8A85" VerticalAlignment="Center" HorizontalAlignment="Center"/>
											</Grid>
										</Border>
									</ControlTemplate>
								</ToggleButton.Template>
							</ToggleButton>
							<Popup x:Name="Popup" IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
								<Border Background="White" BorderBrush="#E5E5E0" BorderThickness="1" CornerRadius="8" Margin="0,4,0,0" MinWidth="{TemplateBinding ActualWidth}">
									<Border.Effect>
										<DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="14" ShadowDepth="2"/>
									</Border.Effect>
									<ScrollViewer Margin="4">
										<ItemsPresenter/>
									</ScrollViewer>
								</Border>
							</Popup>
						</Grid>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
			<Setter Property="ItemContainerStyle">
				<Setter.Value>
					<Style TargetType="ComboBoxItem">
						<Setter Property="Padding" Value="16,8" />
						<Setter Property="HorizontalContentAlignment" Value="Left" />
						<Setter Property="Template">
							<Setter.Value>
								<ControlTemplate TargetType="ComboBoxItem">
									<Border x:Name="ItemBd" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
										<ContentPresenter />
									</Border>
									<ControlTemplate.Triggers>
										<Trigger Property="IsHighlighted" Value="True"><Setter TargetName="ItemBd" Property="Background" Value="#F1F8F3"/></Trigger>
										<Trigger Property="IsSelected" Value="True"><Setter TargetName="ItemBd" Property="Background" Value="{StaticResource AccentGreen}"/><Setter Property="Foreground" Value="White"/></Trigger>
									</ControlTemplate.Triggers>
								</ControlTemplate>
							</Setter.Value>
						</Setter>
					</Style>
				</Setter.Value>
			</Setter>
		</Style>

		<Style TargetType="ComboBox" BasedOn="{StaticResource ModernComboBox}" />

		<Style x:Key="StatCardStyle" TargetType="Border" BasedOn="{StaticResource CardStyle}">
			<Setter Property="Padding" Value="16,14"/>
		</Style>

		<Style x:Key="PageNavBtn" TargetType="Button">
			<Setter Property="Background" Value="White"/>
			<Setter Property="Foreground" Value="#374151"/>
			<Setter Property="BorderBrush" Value="#E5EAE7"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="Padding" Value="12,6"/>
			<Setter Property="Cursor" Value="Hand"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
							<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter TargetName="bd" Property="Background" Value="#F3F4F6"/>
							</Trigger>
							<Trigger Property="IsEnabled" Value="False">
								<Setter TargetName="bd" Property="Opacity" Value="0.35"/>
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
	</UserControl.Resources>

	<Grid Margin="20" Background="{StaticResource PageBg}">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- 1. Header with Right-Aligned Report Selector -->
		<Grid Grid.Row="0" Margin="0,0,0,18">
			<TextBlock Text="Reports &amp; Business Analytics" FontSize="22" FontWeight="Bold" Foreground="{StaticResource HeadingText}" VerticalAlignment="Center"/>

			<StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
				<TextBlock Text="Reports :" FontSize="15" FontWeight="Bold" Foreground="{StaticResource HeadingText}" VerticalAlignment="Center" Margin="0,0,10,0"/>
				<ComboBox x:Name="CmbReportSelector" Width="320" Height="38" VerticalAlignment="Center" SelectionChanged="CmbReportSelector_SelectionChanged">
					<ComboBoxItem Content="📄  Sales &amp; Billing Report" Tag="Sales" IsSelected="True"/>
					<ComboBoxItem Content="📦  Stock Valuation &amp; Expiry" Tag="Stock"/>
					<ComboBoxItem Content="💳  Udhar &amp; Customer Ledger" Tag="Udhar"/>
					<ComboBoxItem Content="📋  GST Tax Summary" Tag="GST"/>
					<ComboBoxItem Content="🛍️  Purchases &amp; Supplier Report" Tag="Purchase"/>
				</ComboBox>
			</StackPanel>
		</Grid>

		<!-- 2. Stat Cards Row -->
		<Grid Grid.Row="1" Margin="0,0,0,14">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="14"/>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="14"/>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="14"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>

			<!-- Card 1 -->
			<Border Grid.Column="0" Style="{StaticResource StatCardStyle}">
				<StackPanel Orientation="Horizontal">
					<Border Width="42" Height="42" CornerRadius="21" Background="{StaticResource StatIconBg}" Margin="0,0,12,0" VerticalAlignment="Center">
						<TextBlock x:Name="TxtStat1Icon" Text="📄" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
					</Border>
					<StackPanel VerticalAlignment="Center">
						<TextBlock x:Name="TxtStat1Title" Text="Total Bills (Today)" FontSize="12" Foreground="{StaticResource MutedText}"/>
						<TextBlock x:Name="TxtStat1Value" Text="0" FontSize="20" FontWeight="Bold" Foreground="{StaticResource HeadingText}" Margin="0,2,0,2"/>
						<TextBlock x:Name="TxtStat1Sub" Text="Total invoices generated" FontSize="11" Foreground="{StaticResource MutedText}"/>
					</StackPanel>
				</StackPanel>
			</Border>

			<!-- Card 2 -->
			<Border Grid.Column="2" Style="{StaticResource StatCardStyle}">
				<StackPanel Orientation="Horizontal">
					<Border Width="42" Height="42" CornerRadius="21" Background="{StaticResource StatIconBg}" Margin="0,0,12,0" VerticalAlignment="Center">
						<TextBlock x:Name="TxtStat2Icon" Text="₹" FontSize="18" FontWeight="Bold" Foreground="{StaticResource AccentGreen}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
					</Border>
					<StackPanel VerticalAlignment="Center">
						<TextBlock x:Name="TxtStat2Title" Text="Total Revenue (Today)" FontSize="12" Foreground="{StaticResource MutedText}"/>
						<TextBlock x:Name="TxtStat2Value" Text="₹ 0.00" FontSize="20" FontWeight="Bold" Foreground="{StaticResource HeadingText}" Margin="0,2,0,2"/>
						<TextBlock x:Name="TxtStat2Sub" Text="Total amount collected" FontSize="11" Foreground="{StaticResource MutedText}"/>
					</StackPanel>
				</StackPanel>
			</Border>

			<!-- Card 3 -->
			<Border Grid.Column="4" Style="{StaticResource StatCardStyle}">
				<StackPanel Orientation="Horizontal">
					<Border Width="42" Height="42" CornerRadius="21" Background="{StaticResource StatIconBg}" Margin="0,0,12,0" VerticalAlignment="Center">
						<TextBlock x:Name="TxtStat3Icon" Text="📊" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
					</Border>
					<StackPanel VerticalAlignment="Center">
						<TextBlock x:Name="TxtStat3Title" Text="Total GST (Today)" FontSize="12" Foreground="{StaticResource MutedText}"/>
						<TextBlock x:Name="TxtStat3Value" Text="₹ 0.00" FontSize="20" FontWeight="Bold" Foreground="{StaticResource HeadingText}" Margin="0,2,0,2"/>
						<TextBlock x:Name="TxtStat3Sub" Text="Total GST amount" FontSize="11" Foreground="{StaticResource MutedText}"/>
					</StackPanel>
				</StackPanel>
			</Border>

			<!-- Card 4 -->
			<Border Grid.Column="6" Style="{StaticResource StatCardStyle}">
				<StackPanel Orientation="Horizontal">
					<Border Width="42" Height="42" CornerRadius="21" Background="{StaticResource StatIconBg}" Margin="0,0,12,0" VerticalAlignment="Center">
						<TextBlock x:Name="TxtStat4Icon" Text="📈" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
					</Border>
					<StackPanel VerticalAlignment="Center">
						<TextBlock x:Name="TxtStat4Title" Text="Average Bill Value" FontSize="12" Foreground="{StaticResource MutedText}"/>
						<TextBlock x:Name="TxtStat4Value" Text="₹ 0.00" FontSize="20" FontWeight="Bold" Foreground="{StaticResource HeadingText}" Margin="0,2,0,2"/>
						<TextBlock x:Name="TxtStat4Sub" Text="Average per invoice" FontSize="11" Foreground="{StaticResource MutedText}"/>
					</StackPanel>
				</StackPanel>
			</Border>
		</Grid>

		<!-- 3. Main Report Body -->
		<Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="20">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="*"/>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
				</Grid.RowDefinitions>

				<!-- Filter & Action Bar -->
				<Grid Grid.Row="0" Margin="0,0,0,16">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*"/>
						<ColumnDefinition Width="Auto"/>
					</Grid.ColumnDefinitions>

					<!-- Date Range -->
					<StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center">
						<TextBlock Text="Date Range:" VerticalAlignment="Center" FontSize="13" FontWeight="SemiBold" Margin="0,0,10,0"/>
						<ComboBox x:Name="CmbDateRange" Width="170" Height="36" VerticalContentAlignment="Center" SelectionChanged="DateRange_Changed">
							<ComboBoxItem Content="Today" IsSelected="True"/>
							<ComboBoxItem Content="This Week"/>
							<ComboBoxItem Content="This Month"/>
							<ComboBoxItem Content="Last 30 Days"/>
							<ComboBoxItem Content="This Year"/>
							<ComboBoxItem Content="All Time"/>
						</ComboBox>
						<Button x:Name="BtnRefresh" Style="{StaticResource RefreshButton}" Height="38" Width="112" Margin="10,0,0,0" Click="BtnRefresh_Click"/>
					</StackPanel>

					<!-- Actions Dropdown -->
					<ComboBox x:Name="CmbActions" Grid.Column="1" Width="180" Height="36" Style="{StaticResource ModernComboBox}" SelectionChanged="CmbActions_SelectionChanged" Padding="16,6,8,6" HorizontalContentAlignment="Left">
						<ComboBoxItem Content="Actions" Tag="" IsSelected="True" IsEnabled="False" />
						<ComboBoxItem Content="📊 Export CSV" Tag="Export" />
						<ComboBoxItem Content="💾 Save PDF" Tag="SavePdf" />
						<ComboBoxItem Content="🖨️ Print Report" Tag="Print" />
					</ComboBox>
				</Grid>

				<!-- Dynamic DataGrid Report View -->
				<DataGrid x:Name="GridReport" Grid.Row="1" Style="{StaticResource PremiumDataGridStyle}"
						  CanUserResizeColumns="True" AutoGeneratingColumn="GridReport_AutoGeneratingColumn"
						  HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
						  HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
						  ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto"/>

				<!-- Pagination Bar -->
				<Grid Grid.Row="2" Margin="0,12,0,0">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*"/>
						<ColumnDefinition Width="Auto"/>
					</Grid.ColumnDefinitions>

					<StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center">
						<TextBlock x:Name="TxtShowingInfo" VerticalAlignment="Center" Foreground="{StaticResource MutedText}" Margin="0,0,12,0" Text="Showing 0 to 0 of 0 entries"/>
						<TextBlock Text="Show" VerticalAlignment="Center" FontSize="13" Foreground="{StaticResource MutedText}" Margin="0,0,6,0"/>
						<ComboBox x:Name="CmbPageSize" Width="70" Height="30" SelectionChanged="PageSize_Changed">
							<ComboBoxItem Content="10" IsSelected="True"/>
							<ComboBoxItem Content="25"/>
							<ComboBoxItem Content="50"/>
							<ComboBoxItem Content="100"/>
						</ComboBox>
						<TextBlock Text="entries" VerticalAlignment="Center" FontSize="13" Foreground="{StaticResource MutedText}" Margin="6,0,0,0"/>
					</StackPanel>

					<StackPanel Orientation="Horizontal" Grid.Column="1">
						<Button x:Name="BtnPrevPage" Content="Previous" Style="{StaticResource PageNavBtn}" Click="BtnPrevPage_Click" Margin="0,0,8,0"/>
						<Border Background="{StaticResource AccentGreen}" CornerRadius="6" Padding="12,6" Margin="0,0,8,0">
							<TextBlock x:Name="TxtPageInfo" Text="1" Foreground="White" FontWeight="Bold" FontSize="13"/>
						</Border>
						<Button x:Name="BtnNextPage" Content="Next" Style="{StaticResource PageNavBtn}" Click="BtnNextPage_Click"/>
					</StackPanel>
				</Grid>

				<!-- Summary Footer -->
				<Border Grid.Row="3" Background="#F8FAF9" BorderBrush="#E5EAE7" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,14,0,0">
					<Grid>
						<Border Width="4" Background="{StaticResource AccentGreen}" HorizontalAlignment="Left" CornerRadius="2"/>
						<TextBlock x:Name="TxtReportSummary" Text="Sales &amp; Billing Report (Today): 0 Bills | Total Revenue: ₹ 0.00 | Total GST: ₹ 0.00" FontSize="13" FontWeight="Bold" Foreground="#1B5E20" VerticalAlignment="Center" Margin="12,0,0,0"/>
						<TextBlock x:Name="TxtReportFooter" Text="Report generated dynamically from krushibill.db" FontSize="12" Foreground="{StaticResource MutedText}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
					</Grid>
				</Border>
			</Grid>
		</Border>

	</Grid>
</UserControl>
