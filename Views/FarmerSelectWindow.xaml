<Window x:Class="KrushiBillERP.Views.FarmerSelectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KrushiBillERP.Views"
        mc:Ignorable="d"
        Title="Select Customer / Farmer" Height="560" Width="780" 
        WindowStartupLocation="CenterOwner" Background="#F4F6F5">
    <Window.Resources>
        <SolidColorBrush x:Key="AccentGreen" Color="#2E7D32"/>
        <SolidColorBrush x:Key="TextDark" Color="#1E2422"/>
        
        <Style x:Key="SelectButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentGreen}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1B5E20"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Select Customer / Farmer" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextDark}"/>
                <TextBlock Text="Search and select a farmer for the payment receipt" FontSize="12" Foreground="#666666" Margin="0,4,0,0"/>
            </StackPanel>
            <Button Grid.Column="1" Content="✕" Width="30" Height="30" Background="Transparent" BorderThickness="0" FontSize="16" Click="BtnCancel_Click" Cursor="Hand" Foreground="#666666" ToolTip="Close"/>
        </Grid>

        <!-- Search Bar -->
        <Border Grid.Row="1" Background="White" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,15">
            <Grid>
                <TextBlock x:Name="TxtSearchPlaceholder" Text="St Search by name, mobile number or village..." Foreground="#999999" VerticalAlignment="Center" IsHitTestVisible="False" Margin="4,0,0,0"/>
                <TextBox x:Name="TxtSearch" Background="Transparent" BorderThickness="0" VerticalAlignment="Center" KeyUp="TxtSearch_KeyUp"/>
            </Grid>
        </Border>

        <!-- DataGrid: use shared premium table Style for consiStent look -->
        <DataGrid x:Name="GridFarmers" Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  Style="{StaticResource PremiumDataGridStyle}" ColumnWidth="*" RowHeight="52" AlternationCount="2"
                  MouseDoubleClick="GridFarmers_MouseDoubleClick">

            <DataGrid.Resources>
                <Style x:Key="CellTextBlockCenter" TargetType="TextBlock">
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                </Style>
                <Style x:Key="CellTextBlockCenterBoth" TargetType="TextBlock" BasedOn="{StaticResource CellTextBlockCenter}">
                    <Setter Property="TextAlignment" Value="Center"/>
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="No" Binding="{Binding FarmerId}" Width="60" ElementStyle="{StaticResource CellTextBlockCenterBoth}" />
                <DataGridTemplateColumn Header="Farmer Name" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding FarmerName}" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="{StaticResource AccentGreen}" TextTrimming="CharacterEllipsis" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Mobile" Binding="{Binding MobileNumber}" Width="150" ElementStyle="{StaticResource CellTextBlockCenter}" />
                <DataGridTextColumn Header="Village" Binding="{Binding VillageName}" Width="150" ElementStyle="{StaticResource CellTextBlockCenter}" />
                <DataGridTemplateColumn Header="Action" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Select" Style="{StaticResource SelectButton}" Click="BtnSelect_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Cancel" Style="{DynamicResource SecondaryButton}" Click="BtnCancel_Click" Width="100" Padding="0,8" Background="White" BorderBrush="#DDDDDD" BorderThickness="1"/>
        </StackPanel>
    </Grid>
</Window>
