using System.Windows.Controls;

namespace KrushiBillERP.Views
{
    // TODO: DatabaseHelper.cs ma is module (table + CRUD methods) mate code umero,
    // pachi aa page ma UI + logic (jem ProductsView / BillingView ma che tem) banavo.
    public partial class SuppliersView : Page
    {
        public SuppliersView()
        {
            InitializeComponent();
        }
    }
}
